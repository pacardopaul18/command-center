Standard button for all actions; navy primary (one per view), bordered secondary, quiet ghost.

```jsx
<Button onClick={save}>Create action item</Button>
<Button variant="secondary">Cancel</Button>
<Button variant="ghost" size="sm">View all</Button>
```

Labels are sentence case, plain verbs. No em dashes, no emoji.
