Default container for grouped content. Cream callout variant for highlights and empty states.

```jsx
<Card title="What will slip" action={<Button variant="ghost" size="sm">View all</Button>}>…rows…</Card>
<Card callout>No meetings today.</Card>
```
