Status pill used on every record. Fixed vocabulary: on track, at risk, blocked, done, overdue, waiting, open.

```jsx
<StatusChip status="atrisk" />
<StatusChip status="done" />
```

Never invent new statuses or colors; gold carries risk, green carries positive.
